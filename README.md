# EmotionMetrics — Staging / Ensayo

Repositorio público de **pruebas** para revisar cambios visuales del sitio emotionmetrics.ai antes de publicarlos en producción.

- Vista en vivo: se activa GitHub Pages sobre `main` — link abajo.
- Este repo se actualiza automáticamente cada vez que se hace un commit en el repositorio de trabajo local ("CLAUDE CODE TEST").
- **No es el repo de producción.** El repo real y privado es `sergioanselmi/emotionmetrics`, con su propia infraestructura de deploy (no incluida acá por seguridad).
